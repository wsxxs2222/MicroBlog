<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post</title>
</head>
<body>
    <h1>post</h1>
    <div class="post">
        <div class="post-title">
            <% if (locals.title) {%> 
            <%= title %>
            <% } %>
        </div>
        <div class="text">
            <% if (locals.content) {%> 
            <%= content %> 
            <% } %>
        </div>
        <div class="image">

        </div>
    </div>
</body>
</html>